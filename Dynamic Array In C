#include <stdio.h>
#include <stdlib.h>

// Stores the total number of books in each shelf
int* total_number_of_books;

// Stores the total number of pages in each book of each shelf
int** total_number_of_pages;

int main() {
    int total_number_of_shelves;
    if (scanf("%d", &total_number_of_shelves) != 1) return 0;
    
    int total_number_of_queries;
    if (scanf("%d", &total_number_of_queries) != 1) return 0;

    // Initialize the arrays
    total_number_of_books = (int*)calloc(total_number_of_shelves, sizeof(int));
    total_number_of_pages = (int**)calloc(total_number_of_shelves, sizeof(int*));

    while (total_number_of_queries--) {
        int type_of_query;
        scanf("%d", &type_of_query);
        
        if (type_of_query == 1) {
            int x, y;
            scanf("%d %d", &x, &y);
            
            // Increment number of books
            *(total_number_of_books + x) += 1;
            
            // Reallocate memory to accommodate new book
            *(total_number_of_pages + x) = realloc(*(total_number_of_pages + x), 
                                                *(total_number_of_books + x) * sizeof(int));
            
            // Add the book (y pages) at the end
            *(*(total_number_of_pages + x) + *(total_number_of_books + x) - 1) = y;
        } else if (type_of_query == 2) {
            int x, y;
            scanf("%d %d", &x, &y);
            // Output pages in specific book
            printf("%d\n", *(*(total_number_of_pages + x) + y));
        } else {
            int x;
            scanf("%d", &x);
            // Output total books in shelf
            printf("%d\n", *(total_number_of_books + x));
        }
    }

    // Free memory
    if (total_number_of_books) {
        free(total_number_of_books);
    }
    for (int i = 0; i < total_number_of_shelves; i++) {
        if (*(total_number_of_pages + i)) {
            free(*(total_number_of_pages + i));
        }
    }
    if (total_number_of_pages) {
        free(total_number_of_pages);
    }
    
    return 0;
}
